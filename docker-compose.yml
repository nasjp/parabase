version: '3.4'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile-app
    volumes:
        - ./:/app:delegated
    working_dir: /app
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7.34
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=password
